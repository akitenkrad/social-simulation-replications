[project]
name = "{{NAME}}-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "{{NAME}}-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
{{NAME}}-tools = { workspace = true }
